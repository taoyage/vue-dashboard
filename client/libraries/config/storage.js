'use strict';
/**
 * Storage
 *
 * @example
 *   import storage from './storage'
 *   storage.get( ... )
 *   storage.set( ... )
 *   storage.remove( ... )
 *   storage.clearAll()
 *   storage.each( ... )
 */

import store from 'store';

export default store;
